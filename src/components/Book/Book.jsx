
const Book = ({book}) => {
    console.log(book);
    const {author,bookName, category,image,publisher,rating,review,tags,totalPages,yearOfPublishing } = book;
    return (
        <div>
            
        </div>
    );
};

export default Book;